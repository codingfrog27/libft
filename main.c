#include "libft.h"
#include <stdio.h>
#include <stdlib.h>

#include "libft.h"

int	main(void)
{
	int n = -42069;
	char *s = ft_itoa(n);
	printf("my itao is %s\n", s);
	printf("og itoa is %d\n", n);
	return (0);
}
